package com.company.codingscales.interviews.Zillow;

public class F {
    // System Design => Create An api to get an random word when given an ISBN number

    // Eg of Book => Run, Run faster, Keep Running, Run till end

    // The word run appears the most number of times, so the probability that our api returns the word Run should be the most.
    // How to store the book contents in memory? How to return the random element
    // How to scale the application when there are requests up-to 1000 books
}
